import s from "../Dialogs.module.css"






let Message = (props) => {
    debugger
        return (

            <div className={s.messages}>{props.message}</div>

        )

    }

export default Message